#include "main.h"

/**
 * _abs -  computes abs value of an int
 * @a: the int in we are looking for
 *
 * Return: the abs of the int
 */

int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (-a);
}
