#include "main.h"

/**
 * print_rev- prints a string in revrse, followed by a new line
 * @s: input string
 * Return: no return
 */

void print_rev(char *s)
{
	int coun = 0;

	while (count >= 0)
	{
		if (s[cont] == '\0')
			break;
		count++;
	}

	for (count--; count >= 0 ; connt --)
		_putchar(s[count]);
	_putchar('\n');
}
